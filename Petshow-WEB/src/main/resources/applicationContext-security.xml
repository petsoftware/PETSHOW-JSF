<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
             http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

      <global-method-security secured-annotations="enabled" jsr250-annotations="enabled"  />

    <http use-expressions="true" auto-config="true" > 
    
        <!-- <intercept-url pattern="/pages/**" access="isAuthenticated()" /> -->
        <!-- 
        *********************************************************
        		DESCRIÇÃO DAS CONFIGURAÇÕES
        *********************************************************
        @ - pattern : Indica o Padrão de URL que será interceptado pelo Spring Security. A forme de identificação
        do padrão foi definido pelo atributo path-type.
        @ - method  : Metodo de requisição que será considerada válida para permitir o acesso ao recurso. Os valores
        são GET. DELETE, HEAD, OPTIONS e POST. Por padrão qualquer método será aceito.
        @ - acess   : Lista separada por vírgula dos nomes de permissão (ROLE) que serão aceitos para permitir acesso ao
        recruso.
        @ - requires-channel : Permite indicar um protocolo obrigatório para o acesso ao recurso. Os valores pos´oveis
        são: any, http e https. O valor padrão é any  
         -->
        <intercept-url pattern="/pages/**" access="permitAll" />
        <intercept-url pattern="/**" access="permitAll" />
        <form-login login-page="/public/login.xhtml" 
        authentication-failure-url="/semPermissao.xhtml" />
        <!-- <access-denied-handler error-page="/semPermissao.xhtml" />-->

    </http>
    
     

    <authentication-manager alias="authenticationManager" >
        <authentication-provider user-service-ref="userDetailsService" />
    </authentication-manager>

</beans:beans>
